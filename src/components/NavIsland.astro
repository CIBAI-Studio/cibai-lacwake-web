---
import { getMenu, getSiteConfig } from '../lib/cms';

const [navLinks, siteConfig] = await Promise.all([getMenu(), getSiteConfig()]);
const currentPath = Astro.url.pathname;
---

<header class="site-header" role="banner" id="site-header">
  <div class="header-pill">
    <!-- Logo -->
    <a href="/" class="header-logo" aria-label="Lacwake — Inicio">
      <span class="logo-text">Lacwake</span>
    </a>

    <!-- Desktop nav -->
    <nav class="header-nav" aria-label="Navegación principal">
      <ul class="nav-links" role="list">
        {navLinks.map(({ href, label }) => (
          <li>
            <a
              href={href}
              class="nav-link"
              aria-current={currentPath === href ? 'page' : undefined}
            >
              {label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- CTA desktop -->
    <a
      href={siteConfig.whatsappUrl}
      class="header-cta"
      target="_blank"
      rel="noopener noreferrer"
      aria-label={`${siteConfig.whatsappLabelShort} — abre en nueva pestaña`}
    >
      {siteConfig.whatsappLabelShort}
    </a>

    <!-- Mobile hamburger -->
    <button
      class="nav-hamburger"
      id="nav-toggle"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Abrir menú"
    >
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </button>
  </div>

  <!-- Mobile menu (panel desplegable) -->
  <div
    class="mobile-panel"
    id="mobile-menu"
    aria-hidden="true"
    role="dialog"
    aria-label="Menú móvil"
  >
    <div class="mobile-inner">
      <ul class="mobile-list" role="list">
        {navLinks.map(({ href, label }) => (
          <li>
            <a
              href={href}
              class="mobile-link"
              aria-current={currentPath === href ? 'page' : undefined}
            >
              {label}
            </a>
          </li>
        ))}
        <li>
          <a
            href={siteConfig.whatsappUrl}
            class="mobile-cta"
            target="_blank"
            rel="noopener noreferrer"
            aria-label={`${siteConfig.whatsappLabel} — abre en nueva pestaña`}
          >
            {siteConfig.whatsappLabel}
          </a>
        </li>
      </ul>
    </div>
  </div>
</header>

<style>
  /* ── Header shell: wrapper fijo que centra la pill flotante ──────
     `pointer-events: none` deja pasar clics al Hero salvo en la pill/panel
     (que reactivan pointer-events). El header NO empuja el Hero: es fixed. */
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: max(1rem, env(safe-area-inset-top));
    padding-inline: 1rem;
    pointer-events: none;
  }

  /* ── Pill blanca redondeada flotando sobre el Hero ──────────────── */
  .header-pill {
    pointer-events: auto;
    display: flex;
    align-items: center;
    width: 100%;
    max-width: 1080px;
    height: 3.75rem;
    padding-inline: 1.25rem;
    gap: 0.75rem;
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(14px) saturate(180%);
    -webkit-backdrop-filter: blur(14px) saturate(180%);
    border: 1px solid rgba(231, 229, 228, 0.7);
    border-radius: 9999px;
    box-shadow: 0 6px 28px rgba(12, 74, 110, 0.1);
    transition: background 280ms ease, box-shadow 280ms ease;
  }

  .site-header.scrolled .header-pill {
    background: rgba(255, 255, 255, 0.98);
    box-shadow: 0 10px 34px rgba(12, 74, 110, 0.16);
  }

  /* ── Logo ─────────────────────────────────────────────────────── */
  .header-logo {
    text-decoration: none;
    flex-shrink: 0;
    margin-right: auto;
  }

  .logo-text {
    font-family: var(--font-heading);
    font-size: 1.375rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    color: #0f766e; /* teal-700: ≥4.5:1 sobre pill blanca */
  }

  /* ── Desktop nav ──────────────────────────────────────────────── */
  .header-nav {
    display: none;
  }

  @media (min-width: 768px) {
    .header-nav {
      display: block;
    }
  }

  .nav-links {
    display: flex;
    align-items: center;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 0.125rem;
  }

  .nav-link {
    display: block;
    padding: 0.4rem 0.875rem;
    font-size: 0.9375rem;
    font-weight: 500;
    color: #44403c; /* stone-700 sobre blanco */
    text-decoration: none;
    border-radius: 9999px;
    transition: color 180ms ease, background 180ms ease;
  }

  .nav-link:hover,
  .nav-link[aria-current="page"] {
    color: #0f766e;
    background: rgba(15, 118, 110, 0.08);
  }

  /* ── CTA «Reservar ahora» naranja ─────────────────────────────── */
  .header-cta {
    display: none;
    flex-shrink: 0;
    padding: 0.5rem 1.25rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: #1c1917; /* texto oscuro sobre amber → AA */
    background: #f59e0b;
    border-radius: 9999px;
    text-decoration: none;
    min-height: 44px;
    align-items: center;
    white-space: nowrap;
    transition: background 180ms ease, transform 140ms ease;
  }

  @media (min-width: 768px) {
    .header-cta {
      display: flex;
    }
  }

  .header-cta:hover {
    background: #d97706;
    transform: translateY(-1px);
  }

  .header-cta:active {
    transform: translateY(0);
  }

  /* ── Hamburger (móvil) ────────────────────────────────────────── */
  .nav-hamburger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    padding: 0.625rem;
    background: transparent;
    border: none;
    cursor: pointer;
    flex-shrink: 0;
    margin-left: auto;
  }

  @media (min-width: 768px) {
    .nav-hamburger {
      display: none;
    }
  }

  .hamburger-line {
    display: block;
    height: 1.5px;
    background: #292524; /* oscuro sobre pill blanca */
    border-radius: 2px;
    transition: transform 260ms ease, opacity 260ms ease;
  }

  .nav-hamburger[aria-expanded="true"] .hamburger-line:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }
  .nav-hamburger[aria-expanded="true"] .hamburger-line:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }
  .nav-hamburger[aria-expanded="true"] .hamburger-line:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  /* ── Mobile panel: tarjeta redondeada bajo la pill ────────────── */
  .mobile-panel {
    pointer-events: auto;
    width: 100%;
    max-width: 1080px;
    margin-top: 0.5rem;
    overflow: hidden;
    max-height: 0;
    opacity: 0;
    visibility: hidden;
    transition:
      max-height 320ms cubic-bezier(0.16, 1, 0.3, 1),
      opacity 280ms ease,
      visibility 280ms ease;
    background: rgba(255, 255, 255, 0.97);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(231, 229, 228, 0.7);
    border-radius: 1.5rem;
    box-shadow: 0 12px 32px rgba(12, 74, 110, 0.14);
  }

  .mobile-panel[aria-hidden="false"] {
    max-height: 340px;
    opacity: 1;
    visibility: visible;
  }

  .mobile-inner {
    padding-inline: 1.25rem;
  }

  .mobile-list {
    list-style: none;
    margin: 0;
    padding: 0.625rem 0 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }

  .mobile-link {
    display: flex;
    align-items: center;
    padding: 0.75rem 0;
    font-size: 1rem;
    font-weight: 500;
    color: #292524;
    text-decoration: none;
    min-height: 44px;
    transition: color 150ms ease;
  }

  .mobile-link:hover,
  .mobile-link[aria-current="page"] {
    color: #0f766e;
  }

  .mobile-cta {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 0.625rem;
    padding: 0.875rem 1rem;
    font-size: 1rem;
    font-weight: 600;
    color: #1c1917;
    background: #f59e0b;
    border-radius: 0.875rem;
    text-decoration: none;
    min-height: 48px;
    transition: background 150ms ease;
  }

  .mobile-cta:hover {
    background: #d97706;
  }
</style>

<script>
  const header = document.getElementById('site-header');
  const toggle = document.getElementById('nav-toggle');
  const mobileMenu = document.getElementById('mobile-menu');

  function handleScroll() {
    if (!header) return;
    header.classList.toggle('scrolled', window.scrollY > 60);
  }

  window.addEventListener('scroll', handleScroll, { passive: true });
  handleScroll();

  toggle?.addEventListener('click', () => {
    const isOpen = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!isOpen));
    mobileMenu?.setAttribute('aria-hidden', String(isOpen));
  });

  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      toggle?.setAttribute('aria-expanded', 'false');
      mobileMenu.setAttribute('aria-hidden', 'true');
    });
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && toggle?.getAttribute('aria-expanded') === 'true') {
      toggle.setAttribute('aria-expanded', 'false');
      mobileMenu?.setAttribute('aria-hidden', 'true');
      toggle.focus();
    }
  });
</script>
